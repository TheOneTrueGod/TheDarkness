import BattleMap from "./BattleMap";
import { TileCoord } from "../../../../src/battle/BattleTypes";

export default {
    isTileEmpty(battleMap: BattleMap, tileCoord: TileCoord): boolean {
        return true;
    }
};